"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var os = require("os");
var path = require("path");
var functions_1 = require("./functions");
// Sample files
exports.TEST_CODE_JS = 'function test() { Logger.log(\'test\'); }';
exports.TEST_APPSSCRIPT_JSON = JSON.stringify({ timeZone: 'America/New_York' });
// Travis Env Variables
exports.IS_PR = (process.env.TRAVIS_PULL_REQUEST === 'true');
exports.SCRIPT_ID = process.env.SCRIPT_ID || '';
var PROJECT_ID = process.env.PROJECT_ID || '';
var HOME = process.env.HOME || '';
// Paths
exports.CLASP_PATHS = {
    clientCredsLocal: 'client_credentials.json',
    rcGlobal: path.join(HOME, '.clasprc.json'),
    rcLocal: '.clasprc.json',
    settingsLocal: '.clasp.json',
};
// Other constants
exports.CLASP = (os.type() === 'Windows_NT') ? 'clasp.cmd' : 'clasp';
exports.CLASP_USAGE = 'Usage: clasp <command> [options]';
var VALID_CLASP_SETTINGS = {
    scriptId: exports.SCRIPT_ID,
    projectId: PROJECT_ID,
};
var INVALID_CLASP_SETTINGS = {
    scriptId: exports.SCRIPT_ID,
    projectId: "project-id-" + functions_1.rndStr(),
};
exports.CLASP_SETTINGS = {
    valid: JSON.stringify(VALID_CLASP_SETTINGS),
    invalid: JSON.stringify(INVALID_CLASP_SETTINGS),
};
var FAKE_CLASPRC_TOKEN = {
    access_token: functions_1.rndStr(),
    refresh_token: functions_1.rndStr(),
    scope: 'https://www.googleapis.com/auth/script.projects',
    token_type: 'Bearer',
    expiry_date: (new Date()).getTime(),
};
var oAuth2ClientOptions = {
    clientId: functions_1.rndStr() + ".apps.googleusercontent.com",
    clientSecret: functions_1.rndStr(),
};
var FAKE_CLASPRC_LOCAL = {
    token: FAKE_CLASPRC_TOKEN,
    oauth2ClientSettings: oAuth2ClientOptions,
    isLocalCreds: true,
};
exports.FAKE_CLASPRC = {
    local: JSON.stringify(FAKE_CLASPRC_LOCAL),
    token: JSON.stringify(FAKE_CLASPRC_TOKEN),
};
var FAKE_CLIENT_CREDS = {
    installed: {
        client_id: functions_1.rndStr() + ".apps.googleusercontent.com",
        client_secret: functions_1.rndStr(),
    },
};
var INVALID_CLIENT_CREDS = {
    installed: {
        client_id: functions_1.rndStr() + ".apps.googleusercontent.com",
    },
};
exports.CLIENT_CREDS = {
    fake: JSON.stringify(FAKE_CLIENT_CREDS),
    invalid: JSON.stringify(INVALID_CLIENT_CREDS),
};
